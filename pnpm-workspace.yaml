packages:
  - "apps/*"
  - "packages/*"

onlyBuiltDependencies:
  # Added by the `Encryption at rest (mobile)` CI job only, never committed as a
  # dependency: it builds natively and a Windows install would fail on it.
  - better-sqlite3-multiple-ciphers
  # appdmg's native helpers, needed by the macOS DMG maker
  - macos-alias
  - fs-xattr
  - "@nestjs/core"
  - "@scarf/scarf"
  - "@swc/core"
  - "core-js"
  - "core-js-pure"
  - "cpu-features"
  - "esbuild"
  - "electron"
  - "msw"
  - "protobufjs"
  - "ssh2"
  - "typesense-instantsearch-adapter"
