# openkeep Produktion (openkeep.de) – Vorlage für den Dokploy Environment-Tab
#
# NICHT als .env committen! Werte in Dokploy unter
# Projekt -> Compose-Service -> Environment eintragen.
#
# Secrets generieren:  openssl rand -base64 32

# --- Datenbank & Objektspeicher (interne Dienste, nie öffentlich) ---
POSTGRES_PASSWORD=
MINIO_ROOT_USER=openkeep-admin
MINIO_ROOT_PASSWORD=

# --- Auth (jeweils min. 32 Zeichen) ---
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=

# --- Initialer Owner-Account ---
OWNER_EMAIL=marcel.pochert@berenberg.de
OWNER_PASSWORD=
OWNER_NAME=Marcel

# --- Mistral (einziger KI-/OCR-Provider, kein lokaler Fallback) ---
MISTRAL_API_KEY=

# --- Optional (Defaults siehe docker-compose.prod.yml) ---
# IMAGE_TAG=latest            # oder ein Commit-SHA für Pinning/Rollback
# Base URL for all Mistral surfaces (chat, embeddings, files, OCR).
# MISTRAL_API_BASE_URL=https://api.mistral.ai
# OCR-only base URL override for split chat/OCR gateways (deprecated; empty = shared URL).
# MISTRAL_OCR_BASE_URL=
# MISTRAL_MODEL=mistral-large-latest
# MISTRAL_OCR_MODEL=mistral-ocr-latest
# OCR request options: blocks, table format, confidence scores, header/footer
# extraction, and the upload transport (auto | inline | files).
# MISTRAL_OCR_INCLUDE_BLOCKS=true
# MISTRAL_OCR_TABLE_FORMAT=markdown
# MISTRAL_OCR_CONFIDENCE_GRANULARITY=page
# MISTRAL_OCR_EXTRACT_HEADER_FOOTER=true
# MISTRAL_OCR_UPLOAD_STRATEGY=auto
# Structured document annotations as an extraction hint (skips up to three chat
# calls per document). See docs/technical/agentic-document-intelligence.md.
# MISTRAL_OCR_DOCUMENT_ANNOTATIONS=false
# MISTRAL_EMBEDDING_MODEL=mistral-embed
# Minimum chunk relevance for RAG answers (0-1); score distributions differ per
# embedding provider, tune when switching providers.
# ANSWER_MIN_CHUNK_SCORE=0.4
# LOG_LEVEL=info

# Hard timeouts for LLM requests (seconds). A hung provider otherwise blocks the
# SSE response until the socket dies.
# LLM_TIMEOUT_SECONDS=45
# LLM_STREAM_TIMEOUT_SECONDS=120

# --- Monitoring (empfohlen) ---
# Dead-man's-switch pro Prozess: zwei Checks auf https://healthchecks.io (kostenlos)
# anlegen, Period = 1 min, Grace = 5 min, und die Ping-URLs hier eintragen.
# API und Worker pingen jede Minute, nachdem sie Postgres, MinIO und die Queue
# geprüft haben. Bleibt ein Ping aus oder schlägt die Prüfung fehl, alarmiert
# healthchecks.io. Details: docs/operations/monitoring-and-health.md
# HEARTBEAT_URL_API=https://hc-ping.com/<uuid-api>
# HEARTBEAT_URL_WORKER=https://hc-ping.com/<uuid-worker>
# HEARTBEAT_INTERVAL_SECONDS=60
